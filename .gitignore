# Секреты и учётные данные — НИКОГДА не коммитить
.env

# Список каналов — пользовательские данные (правится ботом на сервере).
# В репозитории только шаблон channels.txt.example.
channels.txt

# Сессия Telethon = полный доступ к вашему TG-аккаунту
*.session
*.session-journal

# Логи и рантайм
digest.log
*.log

# Опубликованные дайджесты (история для дедупликации между днями)
history/

# Временный файл атомарной записи списка каналов ботом
channels.tmp

# Разобранные статьи (дедуп научных постов) и превью иллюстраций из --dry-run
paper_history.txt
paper_preview.*

# Python
venv/
__pycache__/
*.pyc

# macOS
.DS_Store
